import 'package:flutter/material.dart';

class StatusPickup extends StatefulWidget {
  const StatusPickup({super.key});

  @override
  State<StatusPickup> createState() => _StatusPickupState();
}

class _StatusPickupState extends State<StatusPickup> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
